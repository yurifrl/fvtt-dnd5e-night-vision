{
  "name": "dnd5e-night-vision",
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude e2e",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "foundry:up": "docker compose up -d",
    "foundry:down": "docker compose down",
    "foundry:logs": "docker compose logs -f",
    "foundry:sync": "docker compose cp . foundry:/data/Data/modules/dnd5e-night-vision"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vitest": "^2.1.0"
  }
}
